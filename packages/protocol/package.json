{
  "name": "@monadeo.com/astrogate-protocol",
  "version": "0.0.5",
  "engines": {
    "node": ">=24"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "build": "tsc -p tsconfig.build.json"
  },
  "bugs": "https://github.com/monadeo/astrogate/issues",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/monadeo/astrogate.git",
    "directory": "packages/protocol"
  },
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "description": "Shared protocol between the Astrogate controller and the Pi companion extension",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "devDependencies": {
    "@types/node": "^26.5.1"
  }
}
