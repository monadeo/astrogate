{
  "name": "@monadeo.com/astrogate-controller",
  "version": "0.0.5",
  "engines": {
    "node": ">=24"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "build": "node esbuild.config.mjs"
  },
  "bugs": "https://github.com/monadeo/astrogate/issues",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/monadeo/astrogate.git",
    "directory": "packages/controller"
  },
  "license": "MIT",
  "type": "module",
  "bin": {
    "astrogate": "./dist/index.js"
  },
  "description": "Astrogate controller: event-driven orchestration of Pi sessions in herdr, driven by GitHub",
  "devDependencies": {
    "@monadeo.com/astrogate-protocol": "workspace:*",
    "@types/node": "^26.5.1",
    "esbuild": "^0.28.2"
  }
}
